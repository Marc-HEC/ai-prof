# Demo sans GPU. Tout tourne sur CPU, ou via des API gratuites.
starlette>=0.40
uvicorn>=0.30
httpx>=0.27
python-multipart>=0.0.9
numpy>=1.26

# STT local sur CPU (inutile si STT_ENGINE=groq)
faster-whisper>=1.1

# TTS local sur CPU (TTS_ENGINE=piper) et voix en ligne (TTS_ENGINE=edge)
piper-tts>=1.2
edge-tts>=6.1

# Lecture des cours en PDF
pypdfium2>=4.30
